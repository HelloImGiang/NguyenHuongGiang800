@model IEnumerable<NguyenHuongGiang800.Models.NHG0800>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NHGName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NHGGender)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NHGName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NHGGender)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NHGId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NHGId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NHGId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
